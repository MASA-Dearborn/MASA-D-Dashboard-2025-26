{
  "name": "masa-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "server": "npm run server --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  }
}
